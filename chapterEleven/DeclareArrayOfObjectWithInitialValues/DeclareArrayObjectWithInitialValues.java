package chapterEleven.DeclareArrayOfObjectWithInitialValues;

public class DeclareArrayObjectWithInitialValues {
    /*
    creating an array of objects in Java, we will declare an array of objects
    providing the initial values
    no creating another class object in this approach.
     no use of the constructor in this method.
     use the array {} notation to write the array of objects
     We will use the "Object" type to create the array of objects.
     */

    /*
    For example, create a class and write the main method.
    Then, create an array arr of the Object type. Write the objects
    inside the {} notation. The objects are CRF, a string value, an
    instance of the Integer class with the value of 2020, Husky another string,
    and another instance of the Integer class with value 2017. Finally,
    print each of the objects using the array indexing method.

     */

}
